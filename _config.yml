name:    Elastics
source:  source
plugins: ./_plugins
baseurl: /elastics

keep_files:
- .git

slim:
  pretty:     true
  sort_attrs: false

asset_pipeline:
  bundle:       false         # Default = true
  compress:     false         # Default = true
  output_path:  assets        # Default = assets
  display_path: elastics/assets  # Default = nil

doc_tree:
  doc_source_path: ./source/doc

